#include <iostream>
using namespace std;
int main() {
  cin.tie(nullptr);
  ios_base::sync_with_stdio(false);
  //texto
  string texto;
  getline(cin, texto);
    for(int i = texto.length()-1;i>=0;i--){
    cout<<texto[i];
  }

  return 0;
}