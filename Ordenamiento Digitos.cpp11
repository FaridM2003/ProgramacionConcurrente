#include <bits/stdc++.h>
#define MAX 1000001
using namespace std;
int ordenar(int);
int main() {
  ios_base::sync_with_stdio(false);
  cin.tie(0);
  cout.tie(0);
    int a,Ar[MAX];
    cin>>a;
    for(int i=0;i<a;i++){
          cin>>Ar[i];
          
      }
sort(Ar,Ar+a);
    for(int i=0;i<a;i++){
         cout<<Ar[i]<<" ";
      }

}